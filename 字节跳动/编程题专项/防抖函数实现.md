# 手写防抖函数

> **来源**：字节跳动一面面试题（掘金面试记录）
> **难度**：⭐⭐
> **考察频率**：85%（前端核心编码题）

---

## 🎯 题目要求

### **原题要求**
手写一个防抖函数 `debounce(fn, delay)`

### **应用场景**
- 搜索框输入联想
- 窗口大小调整
- 表单提交按钮
- 实时保存功能

---

## 💡 参考答案

### **基础实现**

```javascript
/**
 * 防抖函数基础实现
 * @param {Function} fn - 需要防抖处理的函数
 * @param {number} delay - 延迟时间（毫秒）
 * @returns {Function} - 防抖处理后的函数
 */
function debounce(fn, delay) {
  let timer = null;
  
  return function(...args) {
    // 清除之前的定时器
    if (timer) {
      clearTimeout(timer);
    }
    
    // 设置新的定时器
    timer = setTimeout(() => {
      fn.apply(this, args);
    }, delay);
  };
}
```

### **使用示例**

```javascript
// 搜索框输入防抖
const searchInput = document.getElementById('search-input');
const searchResults = document.getElementById('search-results');

const search = debounce((keyword) => {
  console.log(`搜索关键词: ${keyword}`);
  // 实际请求搜索接口...
}, 300);

searchInput.addEventListener('input', (e) => {
  search(e.target.value);
});
```

---

## 🔧 增强版本实现

### **版本1：立即执行防抖**

```javascript
/**
 * 立即执行防抖（第一次立即执行，后续防抖）
 * @param {Function} fn - 需要防抖的函数
 * @param {number} delay - 延迟时间
 * @param {boolean} immediate - 是否立即执行
 * @returns {Function}
 */
function debounce(fn, delay, immediate = false) {
  let timer = null;
  let isInvoked = false;
  
  return function(...args) {
    const context = this;
    
    // 清除之前的定时器
    if (timer) {
      clearTimeout(timer);
    }
    
    // 立即执行逻辑
    if (immediate && !isInvoked) {
      fn.apply(context, args);
      isInvoked = true;
    } else {
      // 延迟执行逻辑
      timer = setTimeout(() => {
        if (!immediate) {
          fn.apply(context, args);
        }
        isInvoked = false;
      }, delay);
    }
  };
}
```

### **版本2：带取消功能的防抖**

```javascript
/**
 * 高级防抖函数（带取消、立即执行、状态查询功能）
 * @param {Function} fn - 需要防抖的函数
 * @param {number} delay - 延迟时间
 * @param {Object} options - 配置选项
 * @returns {Function} - 防抖函数（带额外方法）
 */
function advancedDebounce(fn, delay, options = {}) {
  const {
    immediate = false,    // 是否立即执行
    maxWait = null,       // 最大等待时间
    trailing = true,      // 是否在延迟后调用
    leading = false       // 是否在开始时调用
  } = options;
  
  let timer = null;
  let lastCallTime = 0;   // 最后一次调用时间
  let lastInvokeTime = 0; // 最后一次执行时间
  let lastArgs = null;    // 最后一次参数
  let lastThis = null;    // 最后一次this
  
  // 实际执行函数
  function invokeFunc(time) {
    const args = lastArgs;
    const context = lastThis;
    lastArgs = lastThis = null;
    lastInvokeTime = time;
    fn.apply(context, args);
  }
  
  // 启动定时器
  function startTimer(pendingFunc, wait) {
    return setTimeout(pendingFunc, wait);
  }
  
  // 判断是否可以执行
  function shouldInvoke(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    
    // 第一次调用
    if (lastCallTime === 0) return true;
    
    // 距离上次调用超过延迟时间
    if (timeSinceLastCall >= delay) return true;
    
    // 超过最大等待时间
    if (maxWait !== null && timeSinceLastInvoke >= maxWait) return true;
    
    return false;
  }
  
  // 剩余等待时间
  function remainingWait(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    const timeWaiting = delay - timeSinceLastCall;
    
    return maxWait === null
      ? timeWaiting
      : Math.min(timeWaiting, maxWait - timeSinceLastInvoke);
  }
  
  // 定时器回调
  function timerExpired() {
    const time = Date.now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // 重新启动定时器
    timer = startTimer(timerExpired, remainingWait(time));
  }
  
  // 前缘（开始）处理
  function leadingEdge(time) {
    lastInvokeTime = time;
    timer = startTimer(timerExpired, delay);
    return leading ? invokeFunc(time) : undefined;
  }
  
  // 后缘（结束）处理
  function trailingEdge(time) {
    timer = undefined;
    
    // 只有当我们有 `lastArgs` 时才调用
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = null;
  }
  
  // 主函数
  function debounced(...args) {
    const time = Date.now();
    const isInvoking = shouldInvoke(time);
    
    lastArgs = args;
    lastThis = this;
    lastCallTime = time;
    
    if (isInvoking) {
      if (timer === undefined) {
        return leadingEdge(lastCallTime);
      }
      
      // 立即执行逻辑
      if (immediate) {
        clearTimeout(timer);
        timer = startTimer(timerExpired, delay);
        return invokeFunc(lastCallTime);
      }
    }
    
    if (timer === undefined) {
      timer = startTimer(timerExpired, delay);
    }
    
    return undefined;
  }
  
  // 添加额外方法
  debounced.cancel = function() {
    if (timer !== undefined) {
      clearTimeout(timer);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timer = undefined;
  };
  
  debounced.flush = function() {
    return timer === undefined ? undefined : trailingEdge(Date.now());
  };
  
  debounced.pending = function() {
    return timer !== undefined;
  };
  
  return debounced;
}
```

---

## ⚡ TypeScript 版本

```typescript
interface DebounceOptions {
  leading?: boolean;     // 开始时执行
  trailing?: boolean;    // 结束时执行
  maxWait?: number;      // 最大等待时间
}

interface DebouncedFunction<T extends (...args: any[]) => any> {
  (...args: Parameters<T>): ReturnType<T> | undefined;
  cancel: () => void;    // 取消执行
  flush: () => ReturnType<T> | undefined;  // 立即执行
  pending: () => boolean; // 是否在等待中
}

/**
 * TypeScript防抖函数实现
 */
function debounceTS<T extends (...args: any[]) => any>(
  func: T,
  wait: number,
  options: DebounceOptions = {}
): DebouncedFunction<T> {
  let timeoutId: ReturnType<typeof setTimeout> | null = null;
  let lastArgs: Parameters<T> | null = null;
  let lastThis: any;
  let lastCallTime = 0;
  let lastInvokeTime = 0;
  let result: ReturnType<T> | undefined;
  
  const {
    leading = false,
    maxWait,
    trailing = true
  } = options;
  
  // 是否应该执行
  function shouldInvoke(time: number): boolean {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    
    return (
      lastCallTime === 0 ||
      timeSinceLastCall >= wait ||
      (maxWait !== undefined && timeSinceLastInvoke >= maxWait)
    );
  }
  
  // 实际执行函数
  function invokeFunc(time: number): ReturnType<T> | undefined {
    const args = lastArgs;
    const context = lastThis;
    
    lastArgs = lastThis = null;
    lastInvokeTime = time;
    if (args !== null) {
      result = func.apply(context, args);
    }
    return result;
  }
  
  // 启动定时器
  function startTimer(pendingFunc: () => void, wait: number): ReturnType<typeof setTimeout> {
    return setTimeout(pendingFunc, wait);
  }
  
  // 定时器到期
  function timerExpired(): void {
    const time = Date.now();
    if (shouldInvoke(time)) {
      trailingEdge(time);
      return;
    }
    // 重新启动定时器
    timeoutId = startTimer(timerExpired, remainingWait(time));
  }
  
  // 计算剩余等待时间
  function remainingWait(time: number): number {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    const timeWaiting = wait - timeSinceLastCall;
    
    return maxWait === undefined
      ? timeWaiting
      : Math.min(timeWaiting, maxWait - timeSinceLastInvoke);
  }
  
  // 开始处理
  function leadingEdge(time: number) {
    lastInvokeTime = time;
    timeoutId = startTimer(timerExpired, wait);
    return leading ? invokeFunc(time) : result;
  }
  
  // 结束处理
  function trailingEdge(time: number) {
    timeoutId = null;
    if (trailing && lastArgs !== null) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = null;
    return result;
  }
  
  // 取消执行
  function cancel() {
    if (timeoutId !== null) {
      clearTimeout(timeoutId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timeoutId = null;
  }
  
  // 立即执行
  function flush() {
    return timeoutId === null ? result : trailingEdge(Date.now());
  }
  
  // 是否在等待中
  function pending() {
    return timeoutId !== null;
  }
  
  // 防抖函数主体
  function debounced(...args: Parameters<T>): ReturnType<T> | undefined {
    const time = Date.now();
    const isInvoking = shouldInvoke(time);
    
    lastArgs = args;
    // @ts-ignore
    lastThis = this;
    lastCallTime = time;
    
    if (isInvoking) {
      if (timeoutId === null) {
        return leadingEdge(lastCallTime);
      }
      
      // 立即执行逻辑
      if (maxWait !== undefined) {
        clearTimeout(timeoutId);
        timeoutId = startTimer(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    
    if (timeoutId === null) {
      timeoutId = startTimer(timerExpired, wait);
    }
    
    return result;
  }
  
  // 添加额外方法
  debounced.cancel = cancel;
  debounced.flush = flush;
  debounced.pending = pending;
  
  return debounced;
}

// 使用示例
const debouncedLog = debounceTS(console.log, 300);
debouncedLog('Hello'); // 不会立即执行
setTimeout(() => debouncedLog('World'), 200); // 合并执行
```

---

## 🏢 字节跳动考察点

### **字节面试官想考察什么？**
1. ✅ **基础编码能力**：能否写出正确的防抖逻辑
2. ✅ **this处理能力**：能否正确处理上下文（apply/apply）
3. ✅ **参数处理能力**：能否处理不定参数（...args）
4. ✅ **工程实践能力**：是否考虑到了各种边界情况
5. ✅ **代码设计能力**：能否设计出可扩展的API接口

### **常考追问**
1. **"为什么用clearTimeout而不是clearInterval？"**
   - setTimeout返回的是timeoutID，clearTimeout可以精确取消
   - setInterval是重复执行，逻辑不同

2. **"apply和call的区别是什么？什么时候用哪个？"**
   - apply接受数组参数，call接受参数列表
   - 这里用apply因为args是数组，更方便

3. **"你的防抖函数能处理this丢失吗？"**
   - 通过闭包保存定时器引用
   - 通过apply/call绑定正确的this上下文

4. **"如果要支持立即执行和取消功能，怎么设计？"**
   - 增加配置参数（immediate, maxWait等）
   - 定义额外方法（cancel, flush, pending）

---

## 🛠️ 实际应用场景

### **场景1：表单防重复提交**
```javascript
// 防止按钮重复点击
const submitButton = document.getElementById('submit-btn');
const submitForm = debounce((data) => {
  fetch('/api/submit', {
    method: 'POST',
    body: JSON.stringify(data)
  });
}, 1000, { immediate: true });

submitButton.addEventListener('click', () => {
  const formData = collectFormData();
  submitForm(formData);
});
```

### **场景2：实时搜索优化**
```javascript
// 搜索框智能防抖（首次快速响应，后续防抖）
const searchInput = document.getElementById('search');
const search = advancedDebounce((keyword) => {
  if (keyword.length < 2) return;
  fetch(`/api/search?q=${encodeURIComponent(keyword)}`)
    .then(res => res.json())
    .then(displayResults);
}, 500, {
  leading: true,   // 第一次立即执行
  trailing: true,  // 最后一次也要执行
  maxWait: 2000    // 最多等待2秒必须执行
});

searchInput.addEventListener('input', (e) => {
  search(e.target.value.trim());
});
```

### **场景3：自动保存功能**
```javascript
// 文档自动保存（频繁修改时合并保存）
const editor = document.getElementById('editor');
let unsavedChanges = false;

const autoSave = debounce((content) => {
  saveToBackup(content);
  unsavedChanges = false;
  showSaveIndicator('已保存');
}, 3000, { maxWait: 10000 }); // 最多等待10秒必须保存

editor.addEventListener('input', (e) => {
  unsavedChanges = true;
  autoSave(e.target.value);
  showSaveIndicator('保存中...');
});

// 页面关闭前强制保存
window.addEventListener('beforeunload', () => {
  if (unsavedChanges) {
    autoSave.flush();
  }
});
```

---

## ⚠️ 常见错误与陷阱

### **错误1：忘记处理this**
```javascript
// ❌ 错误：this指向错误
function wrongDebounce(fn, delay) {
  let timer = null;
  return function() {
    clearTimeout(timer);
    timer = setTimeout(fn, delay); // fn中的this丢失！
  };
}

// ✅ 正确：使用apply绑定this
function correctDebounce(fn, delay) {
  let timer = null;
  return function(...args) {
    const context = this; // 保存正确的this
    clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(context, args); // 正确绑定
    }, delay);
  };
}
```

### **错误2：忘记传递参数**
```javascript
// ❌ 错误：参数丢失
function wrongDebounce(fn, delay) {
  let timer = null;
  return function() {
    clearTimeout(timer);
    timer = setTimeout(fn, delay); // 参数去哪了？
  };
}

// ✅ 正确：参数传递
function correctDebounce(fn, delay) {
  let timer = null;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(this, args); // 参数正确传递
    }, delay);
  };
}
```

### **错误3：多次清除定时器**
```javascript
// ❌ 错误：不必要的判断
function wrongDebounce(fn, delay) {
  let timer = null;
  return function() {
    if (timer) {  // 这个判断其实没必要
      clearTimeout(timer);
    }
    timer = setTimeout(fn, delay);
  };
}

// ✅ 正确：直接clearTimeout
function correctDebounce(fn, delay) {
  let timer = null;
  return function() {
    clearTimeout(timer); // clearTimeout对null是安全的
    timer = setTimeout(fn, delay);
  };
}
```

---

## 📝 面试答题技巧

### **回答思路**
1. **先写基础版本**：展示最基本的防抖实现
2. **逐步增强**：加入立即执行、取消等高级功能
3. **解释设计决策**：为什么这么设计（性能、兼容性、易用性）
4. **结合实际场景**：举例说明实际应用
5. **展示测试用例**：如何测试防抖函数的正确性

### **加分项**
1. 实现TypeScript版本（类型安全）
2. 实现单元测试示例
3. 性能优化考虑（清除无效定时器、内存管理）
4. 对比防抖与节流的区别

### **字节面试评分标准**
- ✅ 基础功能正确实现：30分
- ✅ this处理和参数传递：20分  
- ✅ 边界情况处理：20分
- ✅ 代码可读性和设计：15分
- ✅ 扩展功能实现：15分

---

**本题总结**：防抖函数是前端工程师必备的核心编码能力，字节跳动特别注重候选人对this、闭包、定时器等基础概念的深入理解和实际应用能力。